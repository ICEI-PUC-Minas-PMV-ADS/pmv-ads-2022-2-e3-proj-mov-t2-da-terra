import React, { useContext } from "react";
import { ProdutoContext } from "../contexts/ProdutoProvider";

// const { produto, setProduto } = useContext(ProdutoContext);
export const getProdutos = (id) => {
  //A id no caso seria para obter os produtos de acordo com a id do usuário,contudo ,por enquanto,esta trazendo tudo

  //PORT Gabriel:5111
  //PORT Carlos:5097

  
};
